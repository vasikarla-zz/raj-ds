import React from 'react';

import SampleComponent from '.';
import notes from '../README.md';

export default {
  title: 'Components|SampleComponent',
  parameters: { notes }
};

export const BasicUsage = () => <SampleComponent />;
